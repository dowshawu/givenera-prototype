define( function (require) {
    'use strict';

    return Parse.Object.extend({
        className: 'Friends',

        defaults: {
            Status: "unknown"
        }

        //initialize: function () {
        //
        //}
    });
});